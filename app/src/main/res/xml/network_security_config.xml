<?xml version="1.0" encoding="utf-8"?>
<!--
  Torrent-only app. The only cleartext endpoint is the app's own loopback HTTP stream server on
  127.0.0.1; everything else (poster images) is HTTPS. No user-installed CAs are trusted in the
  base config, so a user-added certificate cannot be used to MITM the app's TLS traffic.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
