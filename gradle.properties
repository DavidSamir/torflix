# JVM: 4 GB is enough for this build; Windows default heap is too small for KSP + Compose.
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn

android.useAndroidX=true
android.nonTransitiveRClass=true
android.defaults.buildfeatures.buildconfig=false
android.defaults.buildfeatures.resvalues=false
android.defaults.buildfeatures.shaders=false

kotlin.code.style=official
ksp.incremental=true

# Modules without unit tests must not fail the aggregate `test` task.
systemProp.failOnNoDiscoveredTests=false

# ART on Fire OS 5 (Android 5.1) miscompiles R8 full-mode output: the app segfaults on a
# coroutine thread inside AOT-compiled dex. Full mode buys little on a sideloaded TV app.
android.enableR8.fullMode=false
